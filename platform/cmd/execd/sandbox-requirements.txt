# Python packages available to learner code inside the execd sandbox.
# These back the "Web & Servers" labs (Flask/FastAPI/DB) and the common
# request-making libraries. sqlite3 needs nothing — it's in the stdlib.
#
# Keep this list small and audited: every package here is importable by
# untrusted code, so it widens the attack surface. Pin versions so a rebuild
# is reproducible.
flask==3.1.3
fastapi==0.128.8
uvicorn==0.34.0
httpx==0.28.1
requests==2.32.3
pydantic==2.10.4
pytest==8.3.4
